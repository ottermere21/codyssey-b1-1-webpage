# CONCEPT.md

## 1. HTML (Hypertext Markup Language)
- 웹을 이루는 가장 기초적인 구성 요소. 웹 콘텐츠의 의미와 구조 정의

## 빈 요소
- 닫는 태그 -> "어디서부터 어디까지 감쌀지"
    - `<p>안녕하세요</p>`: `<p>` 상자 안에 "안녕하세요" 텍스트 넣고 닫음
    - `<img src="images/photo.jpg">`: `<img>`는 무언가를 감싼다기 보다 '스티커'에 가까움 -> </img> 없음
        - `<img>`는 안에 텍스트 삽입 X
        - `src="사진 주소"` 속성을 통해 이미지를 그 자리에 그냥 붙여 넣는 역할
        - `<br>`, `<hr>`도 감쌀 내용물이 없으므로 닫는 태그 X


## HTML 요소
- `<!DOCTYPE html>`:  이 문서가 HTML5로 작성되었음을 선언
- `<html> </html>`:
- `<head> </head>`: 
- `<meta charset="utf-8">`:
- `<meta name="viewport" content="width=device-width">`:
- `<title> </title>`:
- `<body> </body>: 

## 시맨틱 태그
- `<h1> ~ <h6>`: 제목 태그


## 2. CSS (Cascading Style Sheets)
- 웹 페이지의 모양/표현


## 3. JS (JavaScript)
- 웹 페이지의 기능/동작